# レンタルサーバーへの設置手順

`caerleon-php-package.zip` を使って、サイトと管理ページをレンタルサーバーで運用する手順です。
WordPress は不要で、**追加費用もかかりません**（サーバー代のみ）。

---

## 1. サーバーを用意する

**PHP が動くレンタルサーバー**であればどこでも動作します。以下は代表的なものです。

| サービス | 月額の目安 | 備考 |
|---|---|---|
| ロリポップ！（ライト） | 約 400 円 | 最も安価。今回の用途には十分 |
| さくらのレンタルサーバ（スタンダード） | 約 500 円 | 老舗で情報が多い |
| エックスサーバー（スタンダード） | 約 1,000 円 | 高速。将来の拡張にも余裕 |
| ConoHa WING（ベーシック） | 約 900 円 | 管理画面が分かりやすい |

**選ぶ際の条件はこれだけです。**

- PHP が使えること（どのプランでも基本的に対応しています）
- 独自ドメインが設定できること
- SSL（https）が無料で使えること

> データベースは使わないため、「MySQL 対応」などは気にしなくて構いません。

---

## 2. ファイルをアップロードする

1. `caerleon-php-package.zip` を解凍します
2. サーバーの管理画面（ファイルマネージャー）か FTP ソフトで、
   公開フォルダ（`public_html` / `www` / ドメイン名のフォルダ など）を開きます
3. 解凍した**中身をすべて**アップロードします

```
public_html/
├── index.html          … サイト本体
├── admin.html          … 管理ページ
├── admin-api.php       … 管理ページの保存処理
├── content.json        … 文章のデータ
├── photos.json         … 写真の表示設定
├── llms.txt / robots.txt / sitemap.xml
├── images/             … 写真
└── assets/             … ロゴ
```

---

## 3. パスワードを変更する（必須）

`admin-api.php` を開き、**14 行目**を書き換えます。

```php
$ADMIN_PASSWORD = 'CHANGE_ME'; // ← ここを変更
```

↓ 推測されにくい文字列に変更してください（例）

```php
$ADMIN_PASSWORD = 'Roppongi-2026-Kx7pQ';
```

> **変更しないと管理ページは動作しません**（安全のため、初期値のままでは保存を受け付けない仕組みにしています）

---

## 4. 書き込み権限を設定する

写真や文章を保存するために、次のフォルダ・ファイルに書き込み権限が必要です。
ファイルマネージャーの「パーミッション変更」から設定します。

| 対象 | 権限 |
|---|---|
| `images/` フォルダ | **705** または **755** |
| `content.json` | **604** または **644** |
| `photos.json` | **604** または **644** |

> うまく保存できない場合は、この権限設定を最初に確認してください。
> それでも駄目な場合のみ `images/` を 707、JSON を 606 にしてください。

---

## 5. 動作を確認する

1. `https://ご自身のドメイン/` … サイトが表示されること
2. `https://ご自身のドメイン/admin.html` … 管理ページが開くこと
3. 管理ページでログイン
   - **1つ目のパスワード**（ページを開くため）：`Cael-9224D5-Night6F`
   - **2つ目のパスワード**（保存するため）：手順 3 で設定したもの
4. 右上の表示が **「Server」** になっていれば PHP モードで動作しています
5. 文章を少し変更して「保存」→ サイトに**即座に**反映されることを確認

---

## 6. 使い方

### 文章を変える

管理ページの「文章の編集」から、以下をすべて編集できます。

トップの表記／コンセプト／ギャラリーの見出し／**料金システム**／住所・営業時間・電話番号／採用情報／フッター

### 写真を差し替える

「写真の管理」で各枠の **「写真を選ぶ」→「保存」**。
**「サイトから消す」**を押すと、その写真だけ非表示にできます（ギャラリーは自動的に詰まります）。

> 差し替えた写真は `_backup_ファイル名` として 1 世代分が自動保存されます。

### GitHub Pages との違い

| | GitHub Pages（現在） | レンタルサーバー（PHP） |
|---|---|---|
| 反映までの時間 | 1〜2 分 | **即座** |
| 必要な設定 | GitHub のトークン | サーバーのパスワードのみ |
| 独自ドメイン | 設定が必要 | 標準で対応 |

---

## 補足：安全のために

- **`admin.html` と `admin-api.php` は検索エンジンに登録されない設定**にしてあります（`robots.txt`）
- 管理ページのパスワードは、ページ内に**そのまま書かれてはいません**（暗号化された値のみ）
- サーバーの管理画面に「アクセス制限（Basic 認証）」の機能があれば、
  `admin.html` にかけておくとさらに安全です
- 写真は **JPEG のみ・8MB まで**しか受け付けない制限を入れてあります
