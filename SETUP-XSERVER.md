# エックスサーバーへの公開手順

WordPress を使わず、**ファイルを置くだけ**でホームページを公開する手順です。
所要時間は **15分程度**、専門知識は不要です。

---

## この方式のいいところ

| | |
|---|---|
| **速い** | WordPress より表示が軽く、速く開きます |
| **簡単** | ファイルを置くだけ。データベースの設定も不要 |
| **編集できる** | 写真の差し替えと文章の書き換えは、専用の管理ページから行えます |
| **すぐ反映** | 保存した瞬間にサイトへ反映されます |

---

## STEP 1　ドメインをサーバーに登録する

エックスサーバーには管理画面が 2 種類あります。使うのは**サーバーパネル**です。

1. **Xserver アカウント**にログイン
2. **「サーバー管理」** という青いボタンをクリック（サーバーパネルが開きます）
3. 左メニュー【ドメイン】の **「ドメイン設定」** をクリック
4. 一覧に取得したドメインが**あれば STEP 2 へ**
5. 無ければ上部の **「ドメイン設定追加」** タブから追加します
   - **「無料独自SSLを利用する」にチェックが入っていること**を必ず確認
   - 「確認画面へ進む」→「追加する」

> **追加した直後は、まだサイトが見られません。**
> 反映に**最大 1 時間**かかります。「無効なURLです」と出ても故障ではありません。

---

## STEP 2　ファイルをアップロードする

### ① 置き場所を開く

1. サーバーパネルの左メニュー【ホームページ】の **「ファイル管理」** をクリック
2. 別画面（ファイルマネージャ）が開きます
3. 左側の一覧から **取得したドメイン名のフォルダ** をクリック
4. その中の **`public_html`** をクリック

**この `public_html` の中が、ホームページの置き場所です。**

### ② 中にあるファイルを消す

`public_html` の中に、エックスサーバーが最初に用意した見本のファイル
（`index.html` や `default_page.png` など）が入っています。

**これらはすべて選択して削除してください。** 空の状態にします。

> 削除するのは `public_html` の**中身**です。`public_html` そのものは消さないでください。

### ③ ファイルを置く

1. お手元の `caerleon-php-package.zip` を**解凍**します
2. 解凍してできた**中身をすべて**、`public_html` の中にアップロードします

アップロードするもの（フォルダも含めてすべて）:

```
index.html          ← ホームページ本体
admin.html          ← 編集用の管理ページ
admin-api.php       ← 保存処理
content.json        ← 文章のデータ
photos.json         ← 写真の表示設定
robots.txt / sitemap.xml / llms.txt
images/             ← 写真のフォルダ
assets/             ← ロゴのフォルダ
```

> `SETUP-PHP.md` と `SETUP-XSERVER.md`（この手順書）は説明書なので、
> アップロードしてもしなくても構いません。

---

## STEP 3　パスワードを変更する（必須）

管理ページを使うために、**必ず**この作業を行ってください。

1. ファイルマネージャで **`admin-api.php`** を右クリック → **「編集」**
2. 上から **14 行目**にこう書かれています

```php
$ADMIN_PASSWORD = 'CHANGE_ME'; // ← 移管後、必ず変更してください
```

3. `CHANGE_ME` の部分を、**推測されにくい文字列**に書き換えます

```php
$ADMIN_PASSWORD = 'Roppongi-2026-Kx7pQ';
```

4. **保存**します。この文字列は必ずメモしてください

> **変更しないと管理ページは動きません。**
> 初期値のままでは、安全のため保存を受け付けない仕組みにしてあります。

---

## STEP 4　書き込みの許可を与える

写真や文章を保存できるようにする設定です。
ファイルマネージャでファイルを右クリック →「パーミッション変更」（または「属性変更」）から設定します。

| 対象 | 数値 |
|---|---|
| `images` フォルダ | **705** |
| `content.json` | **604** |
| `photos.json` | **604** |

> **これは何の設定？**
> 「誰がこのファイルを書き換えてよいか」を決める設定です。
> 管理ページから保存できるようにするために必要です。

うまく保存できない場合のみ、`images` を **707**、JSON を **606** にしてください。

---

## STEP 5　動作を確認する

順番に開いて、それぞれ確認してください。

| # | 開くところ | 確認すること |
|---|---|---|
| 1 | `https://ご自身のドメイン/` | ホームページが表示される |
| 2 | `https://ご自身のドメイン/admin.html` | 管理ページが開く |
| 3 | 管理ページでログイン | 下記の 2 つのパスワードを入力 |
| 4 | 文章を少し変えて「保存」 | サイトに**すぐ**反映される |

### 管理ページのパスワードは 2 つあります

| | 何のため | 何を入力するか |
|---|---|---|
| **1つめ** | ページを開くため | `Cael-9224D5-Night6F` |
| **2つめ** | 保存するため | **STEP 3 で設定した文字列** |

右上の表示が **「Server」** になっていれば、正しく動いています。

---

## 管理ページの使い方

### 写真を差し替える

「写真の管理」の各枠で **「写真を選ぶ」→「保存」**。
**「サイトから消す」**を押すと、その写真だけ非表示にできます。

> 差し替える前の写真は `_backup_ファイル名` として自動保存されます。

### 文章を変える

「文章の編集」から、**44 項目**すべてを変更できます。

トップの表記／コンセプト／ギャラリーの見出し／**料金システム**／
住所・営業時間・電話番号／採用情報／フッター

変更したら一番下の **「文章を保存」** をクリック。

---

## 困ったときは

| 症状 | 原因と対処 |
|---|---|
| 「無効なURLです」 | ドメイン反映待ちです。**最大 1 時間**お待ちください |
| 「保護されていない通信」 | SSL の反映待ちです。同じく最大 1 時間 |
| 見本のページが表示される | `public_html` の中の見本ファイルが残っています。削除してください |
| 管理ページが「Cloud」と表示される | `admin-api.php` がアップロードされていません |
| 保存できない | STEP 3（パスワード）と STEP 4（書き込み許可）を確認してください |
| 写真が表示されない | `images` フォルダごとアップロードされているか確認してください |

---

## より安全にするために

エックスサーバーには**アクセス制限**の機能があります。
管理ページに鍵をかけておくと、より安全です。

1. サーバーパネル【ホームページ】→ **「アクセス制限」**
2. 対象のドメインを選ぶ
3. `admin.html` に対してユーザーとパスワードを設定

> 設定しなくても、管理ページ自体にパスワードがかかっています。
> こちらは「二重の鍵」にしたい場合の追加設定です。

---

## 今のホームページはどうする？

現在の `shomayamamoto-ai.github.io/caerle-on/` は、**そのまま残しておいて構いません**（無料です）。

新しいサイトが問題なく動いていることを 1〜2 週間確認してから、
名刺や SNS のリンクを新しいドメインに差し替えていくのが安全です。
